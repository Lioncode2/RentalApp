<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="StarterApp.Views.ItemDetailPage"
             Title="{Binding Item.Title}">
    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="15">
            <Border Padding="20" StrokeThickness="1">
                <Border.StrokeShape><RoundRectangle CornerRadius="12"/></Border.StrokeShape>
                <VerticalStackLayout Spacing="10">
                    <Label Text="{Binding Item.Title}" FontSize="24" FontAttributes="Bold"/>
                    <Label Text="{Binding Item.DailyRate, StringFormat='£{0}/day'}" FontSize="20" TextColor="{StaticResource Primary}" FontAttributes="Bold"/>
                    <Label Text="{Binding Item.Description}" FontSize="14"/>
                    <Label Text="{Binding Item.Category, StringFormat='Category: {0}'}" FontSize="13" TextColor="Gray"/>
                    <Label Text="{Binding Item.Location, StringFormat='Location: {0}'}" FontSize="13" TextColor="Gray"/>
                </VerticalStackLayout>
            </Border>
            <Button Text="Request Rental" Command="{Binding RequestRentalCommand}" BackgroundColor="{StaticResource Primary}" TextColor="White"/>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
