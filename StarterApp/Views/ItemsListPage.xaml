<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="StarterApp.Views.ItemsListPage"
             Title="Browse Items">
    <RefreshView IsRefreshing="{Binding IsLoading}" Command="{Binding LoadItemsCommand}">
        <CollectionView ItemsSource="{Binding Items}">
            <CollectionView.EmptyView>
                <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center" Spacing="10">
                    <Label Text="No items available" FontSize="18" HorizontalOptions="Center" TextColor="Gray"/>
                    <Label Text="Be the first to list an item!" FontSize="14" HorizontalOptions="Center" TextColor="Gray"/>
                </VerticalStackLayout>
            </CollectionView.EmptyView>
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Border Margin="15,8" Padding="15" StrokeThickness="1">
                        <Border.StrokeShape><RoundRectangle CornerRadius="12"/></Border.StrokeShape>
                        <Grid RowDefinitions="Auto,Auto,Auto" ColumnDefinitions="*,Auto">
                            <Label Grid.Row="0" Grid.Column="0" Text="{Binding Title}" FontSize="16" FontAttributes="Bold"/>
                            <Label Grid.Row="0" Grid.Column="1" Text="{Binding DailyRate, StringFormat='£{0}/day'}" FontSize="16" FontAttributes="Bold" TextColor="{StaticResource Primary}"/>
                            <Label Grid.Row="1" Grid.ColumnSpan="2" Text="{Binding Description}" FontSize="13" TextColor="Gray" MaxLines="2"/>
                            <Label Grid.Row="2" Grid.Column="0" Text="{Binding Category}" FontSize="12" TextColor="Gray" Margin="0,5,0,0"/>
                            <Label Grid.Row="2" Grid.Column="1" Text="{Binding Location}" FontSize="12" TextColor="Gray" Margin="0,5,0,0"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </RefreshView>
</ContentPage>
