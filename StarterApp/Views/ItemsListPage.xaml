<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:StarterApp.ViewModels"
             x:Class="StarterApp.Views.ItemsListPage"
             Title="Items">
    <ContentPage.BindingContext>
        <vm:ItemsListViewModel/>
    </ContentPage.BindingContext>
    <Grid RowDefinitions="Auto,*">
        <Button Grid.Row="0" Text="Refresh" Command="{Binding LoadItemsCommand}" Margin="10"/>
        <CollectionView Grid.Row="1" ItemsSource="{Binding Items}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Margin="10,5" Padding="10">
                        <VerticalStackLayout>
                            <Label Text="{Binding Title}" FontSize="18" FontAttributes="Bold"/>
                            <Label Text="{Binding Description}" FontSize="14"/>
                            <Label Text="{Binding DailyRate, StringFormat='£{0}/day'}" FontSize="14" TextColor="Green"/>
                            <Label Text="{Binding Location}" FontSize="12" TextColor="Gray"/>
                        </VerticalStackLayout>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>
