<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="StarterApp.Views.RentalsPage"
             Title="My Rentals">
    <ScrollView>
        <VerticalStackLayout Padding="15" Spacing="10">

            <Label Text="Incoming Requests" FontSize="18" FontAttributes="Bold" Margin="0,10,0,5"/>

            <CollectionView ItemsSource="{Binding Incoming}">
                <CollectionView.EmptyView>
                    <Label Text="No incoming requests" TextColor="Gray" HorizontalOptions="Center"/>
                </CollectionView.EmptyView>
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,5" Padding="15" StrokeThickness="1">
                            <Border.StrokeShape><RoundRectangle CornerRadius="10"/></Border.StrokeShape>
                            <VerticalStackLayout Spacing="5">
                                <Label Text="{Binding Item.Title}" FontAttributes="Bold" FontSize="16"/>
                                <Label Text="{Binding Status, StringFormat='Status: {0}'}" TextColor="Gray"/>
                                <Label Text="{Binding TotalPrice, StringFormat='Total: £{0:F2}'}"/>
                                <Label Text="{Binding StartDate, StringFormat='From: {0:dd MMM yyyy}'}" TextColor="Gray" FontSize="12"/>
                                <Label Text="{Binding EndDate, StringFormat='To: {0:dd MMM yyyy}'}" TextColor="Gray" FontSize="12"/>
                                <HorizontalStackLayout Spacing="10" Margin="0,5,0,0">
                                    <Button Text="Approve"
                                            BackgroundColor="Green" TextColor="White"
                                            Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.ApproveCommand}"
                                            CommandParameter="{Binding Id}"/>
                                    <Button Text="Reject"
                                            BackgroundColor="Red" TextColor="White"
                                            Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.RejectCommand}"
                                            CommandParameter="{Binding Id}"/>
                                </HorizontalStackLayout>
                            </VerticalStackLayout>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <Label Text="My Outgoing Requests" FontSize="18" FontAttributes="Bold" Margin="0,15,0,5"/>

            <CollectionView ItemsSource="{Binding Outgoing}">
                <CollectionView.EmptyView>
                    <Label Text="No outgoing requests" TextColor="Gray" HorizontalOptions="Center"/>
                </CollectionView.EmptyView>
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,5" Padding="15" StrokeThickness="1">
                            <Border.StrokeShape><RoundRectangle CornerRadius="10"/></Border.StrokeShape>
                            <VerticalStackLayout Spacing="5">
                                <Label Text="{Binding Item.Title}" FontAttributes="Bold" FontSize="16"/>
                                <Label Text="{Binding Status, StringFormat='Status: {0}'}" TextColor="Gray"/>
                                <Label Text="{Binding TotalPrice, StringFormat='Total: £{0:F2}'}"/>
                                <Label Text="{Binding StartDate, StringFormat='From: {0:dd MMM yyyy}'}" TextColor="Gray" FontSize="12"/>
                                <Label Text="{Binding EndDate, StringFormat='To: {0:dd MMM yyyy}'}" TextColor="Gray" FontSize="12"/>
                            </VerticalStackLayout>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
