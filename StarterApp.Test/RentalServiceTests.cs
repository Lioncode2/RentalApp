namespace StarterApp.Test;

// RentalService tests run locally against the database
// CI pipeline tests the repository layer only
public class RentalServicePlaceholderTests
{
    [Fact]
    public void RentalService_Placeholder_Pass()
    {
        Assert.True(true);
    }
}
